4. Criar função para ler ficheiros JSON usando with
    Criar src/utils.py com:
        load_json_file(path: str)
        usar with open(...)
        tratar erros: ficheiro não existe, JSON inválido

5. Criar o ficheiro principal src/__main__.py
    Este ficheiro será executado com:
        uv run python -m src
    Dentro dele:
        Chamar get_args()
        Ler ficheiros JSON com load_json_file()
        Validar com Pydantic
        Carregar o modelo Small_LLM_Model
        Carregar o vocabulário JSON do modelo
        Preparar estrutura para constrained decoding
        Processar cada prompt
        Gerar {prompt, name, parameters}
        Guardar tudo no ficheiro de output usando with open(...)

6. Implementar o algoritmo de escolha da função
    Enviar prompt ao LLM
    Usar constrained decoding para garantir JSON válido
    O modelo deve escolher:
        "name": "fn_xxx"
        "parameters": {...}

7. Implementar o constrained decoding
    Passos:
        Tokenizar o prompt
        Gerar logits token a token
    Em cada passo:
        Determinar tokens válidos para manter JSON correto
        Bloquear tokens inválidos (logits = -inf)
        Continuar até JSON completo
        Validar JSON final

8. Criar o Makefile
    Regras obrigatórias:
        install
        run
        debug
        clean
        lint
        lint-strict (opcional)

9. Criar README.md
    Deve incluir:
        Descrição do projeto
        Instruções de execução
        Explicação do algoritmo
        Decisões de design
        Performance
        Testes
        Exemplos de uso
        Como usaste IA

10. Criar testes (opcional mas recomendado)
    Usar pytest ou unittest
    Testar:
        JSON inválido
        Ficheiros em falta
        Funções com vários parâmetros
        Strings especiais
        Números grandes

11. Validar tudo
    Correr uv run python -m src
    Verificar se cria data/output/function_calling_result.json
    Confirmar que o JSON é válido
    Confirmar que segue o schema

12. Preparar para defesa
    Saber explicar:
        Constrained decoding
        Como escolhes a função
        Como bloqueias tokens inválidos
        Como validas JSON
        Como geres erros