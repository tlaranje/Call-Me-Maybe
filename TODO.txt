================================================================
TODO - call me maybe (code tasks)
================================================================

CODE QUALITY FIXES
------------------
[ ] src/get_args.py
    [ ] Add docstring to get_args() function (PEP 257 / Google style)
    [ ] Add help strings to all argparse arguments (currently empty "")
    [ ] Add description to ArgumentParser

[ ] src/__main__.py
    [ ] Add docstring to main()
    [X] Add return type hint -> None to main()
    [X] Fix: prompts are being joined into one string -- each prompt must
        be processed individually in a loop
    [ ] Load functions_definition.json and validate against FunctionDefinition model
    [ ] Validate each prompt item against Prompt pydantic model
    [ ] Write results to output file (currently nothing is written)

[ ] src/json_utils.py
    [ ] Replace inline comments with proper docstring in load_json_file()


CORE IMPLEMENTATION
-------------------
[ ] Vocabulary loading
    [ ] Call model.get_path_to_vocab_file() (or equivalent)
    [ ] Load vocabulary JSON into a dict: token_id -> token_string

[ ] Generation pipeline (per prompt)
    [ ] Build prompt string that includes function definitions + user prompt
    [ ] Encode prompt with model.encode()
    [ ] Run autoregressive loop token by token:
        [ ] Call get_logits_from_input_ids(current_input_ids)
        [ ] Apply constrained decoding mask
        [ ] Select next token (argmax of valid logits)
        [ ] Append token to input_ids
        [ ] Stop when JSON is complete (closing "}")

[ ] Constrained decoding
    [ ] Track current JSON generation state (what has been generated so far)
    [ ] At each step, determine which tokens are valid given:
        [ ] Current partial JSON string
        [ ] Expected schema (function name, parameter names, types)
    [ ] Set invalid token logits to -inf (float('-inf'))
    [ ] Select only from valid tokens

[ ] Function selection via LLM
    [ ] The LLM must decide which function to call (NOT hardcoded logic)
    [ ] Constrain "name" field to only valid function names from definitions

[ ] Schema enforcement
    [ ] "name" field: only tokens that form a valid function name
    [ ] "parameters" keys: only valid parameter names for chosen function
    [ ] "parameters" values: types must match definition (number -> float,
        string -> str, boolean -> bool)

[ ] Output writing
    [ ] For each prompt, produce object: {prompt, name, parameters}
    [ ] Write full array to data/output/function_calling_results.json
    [ ] Ensure valid JSON (no trailing commas, no comments)


README.md
---------
[ ] First line (italicized): "This project has been created as part of
    the 42 curriculum by <login>"
[ ] Description section
[ ] Instructions section (install, run, debug)
[ ] Resources section + how AI was used
[ ] Algorithm explanation (constrained decoding approach)
[ ] Design decisions
[ ] Performance analysis
[ ] Challenges faced
[ ] Testing strategy
[ ] Example usage


================================================================
!! IMPORTANT - DO NOT IGNORE !!
================================================================

FORBIDDEN - will fail evaluation:
  - src/ must NEVER import torch, transformers, huggingface directly
    (only llm_sdk is allowed as interface to the model)
  - DO NOT use dspy, outlines, or any structured generation library
  - DO NOT use private methods/attributes from llm_sdk (no _underscore)
  - DO NOT hardcode function names or arguments -- solution must work
    with ANY functions_definition.json and ANY input prompts
  - DO NOT rely on the model producing correct JSON by itself via prompting
    alone -- constrained decoding is MANDATORY

CONSTRAINED DECODING is the entire point of this project:
  - 100% valid JSON output is required, not 90%, not 99% -- 100%
  - Achieving this via prompting tricks will be rejected at evaluation
  - You MUST guide the model token-by-token using the vocabulary

MYPY - lint will fail without these flags:
  --warn-return-any --warn-unused-ignores --ignore-missing-imports
  --disallow-untyped-defs --check-untyped-defs
  (already fixed in Makefile -- make sure it stays that way)

ARGPARSE flag name mismatch:
  - PDF says --functions_definition (singular)
  - Your code uses --functions_definitions (plural)
  - Moulinette may call with --functions_definition -- verify and fix

OUTPUT filename:
  - PDF says: function_calling_results.json
  - Your default is: function_calling_result.json (missing 's')
  - Fix the default in get_args.py and Makefile

EVALUATION may include a live code modification:
  - Know every line of your implementation
  - Be ready to explain constrained decoding step by step
  - Peer review before submitting
================================================================